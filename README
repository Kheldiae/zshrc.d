In this directory, place zsh files to be loaded before zsh is run, in the .zshrc context.
Any file that doesn't end with .zsh will be ignored (like this readme).

The ~/.zshrc file can be based on zshrc.template, included here. ZSH_CONFIG_PATH has to point to this directory.
